name: Agent Test

on:
  workflow_dispatch:

jobs:
  agent-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run agent test
        run: |
          echo "Coding Agent GitHub Actions test"
          echo "Repository: $GITHUB_REPOSITORY"
          echo "Commit: $GITHUB_SHA"
          echo "Status: SUCCESS"
